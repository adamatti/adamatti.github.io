# Instructions for AI Agents

- Do not run the development server (`npm run dev`). The user will handle this externally.
- All pages must support both light and dark themes, with the exception of `/resume`.
- All pages must be responsive and optimized for mobile devices.
