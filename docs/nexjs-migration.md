# Next.js Migration Plan

Generated by GitHub Copilot (Claude Haiku 4.5)
Status: Draft

## Current State
- **Next.js**: 13.1.6 (pages router)
- **React**: 19.0.0 (recently upgraded)
- Uses `getStaticProps` for static generation across multiple pages
- Dependency mismatch: `@next/third-parties` is at 16.1.6 while Next.js is at 13.1.6

## Key Findings
The codebase heavily relies on:
- Pages router with dynamic routes (`/pages/blog/[slug].tsx`, `/pages/technologies/[slug].tsx`)
- `getStaticProps` for static generation (15+ instances)
- `useRouter` from `next/router`
- `@next/third-parties` for Google Tag Manager and analytics
- Custom webpack configuration for path aliases and fallbacks

## Two Upgrade Paths

### Option 1: Incremental Pages Router Update (4-6 hours) ⭐ Recommended
Bump to Next.js 14/15, keeping the pages router:
- Update `next` and `@next/third-parties` versions
- Adjust webpack/experimental config as needed
- Test all pages (home, blog, resume, events, jobs, technologies, videos)
- Handle any breaking changes in dependencies

**Effort breakdown:**
- Version bump & dependency updates: 1 hour
- Config adjustments: 1-2 hours  
- Testing & fixes: 2-3 hours

**Benefits:**
- Minimal code changes
- Can be done incrementally
- Provides foundation for later migration

**Risks:**
- Deprecation warnings start appearing
- Missing out on app router performance benefits

### Option 2: Full App Router Migration (1-2 weeks)
Migrate from pages router to app router (the recommended path forward):
- Restructure routing (`/pages` → `/app`)
- Convert all `getStaticProps` to dynamic route segments & `generateStaticParams`
- Update layout hierarchy
- Re-architect data loading strategy
- Update all `next/router` usage to `next/navigation`

**Effort breakdown:**
- Route restructuring: 3-4 days
- Component updates & testing: 2-3 days
- Fixing edge cases: 1-2 days

**Benefits:**
- Future-proof architecture
- Better performance with server components
- Cleaner layouts and nested routing

**Risks:**
- Large refactor with higher testing burden
- May introduce bugs if not careful

## Pages Requiring Updates

### Pages with `getStaticProps`:
- `/pages/index.tsx`
- `/pages/resume.tsx`
- `/pages/events/index.tsx`
- `/pages/jobs/index.tsx`
- `/pages/pet-projects.tsx`
- `/pages/videos.tsx`
- `/pages/technologies/index.tsx`
- `/pages/technologies/[slug].tsx`
- `/pages/blog/index.tsx`
- `/pages/blog/posts/index.tsx`
- `/pages/blog/posts/[slug].tsx`
- `/pages/blog/tags/index.tsx`
- `/pages/blog/tags/[tag].tsx`

### Components/utilities using Next.js APIs:
- `src/pages/_app.tsx` - `useRouter` from next/router
- `src/pages/_document.tsx` - Document structure
- `src/components/next-custom/image.tsx` - Custom Image component
- `src/analytics/analytics.ts` - GTM integration via @next/third-parties

## Recommendation

**Start with Option 1** (Incremental Pages Router Update):
1. Fix the `@next/third-parties` version mismatch first
2. Bump Next.js incrementally (try 14.x first)
3. Run tests and fix any issues
4. Plan Option 2 for later once pages router is stable

This approach:
- Unblocks any critical updates needed
- Provides early warning of compatibility issues
- Keeps the codebase working at all times
- Allows phased migration to app router when time permits

## Timeline
- Option 1: 1 day (if no major issues)
- Option 2: 2-3 weeks (requires dedicated time)
